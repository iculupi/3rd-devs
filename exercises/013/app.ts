import { config } from 'dotenv';
config();

// Main application logic
async function main() {
    try {
        // Implementation
        console.log('Exercise 013 running...');
    } catch (error) {
        console.error('Error:', error);
    }
}

main();